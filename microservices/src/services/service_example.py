'''
Here are some function codes and database queries / mutations.
'''


def sum(db, x, y):

    sum = x + y

    # MongoDB queries and mutations
    # table = db.tableName
    # table.update_one({}, {"key":sum})

    return sum
